import { homedir } from 'os';

export const VERSION = '4.2.5';

export const DEFAULT_KEYPAIR_FILE = `${homedir()}/.config/solana/id.json`;
export const DEFAULT_RPC_ENDPOINT = 'https://api.mainnet-beta.solana.com';
