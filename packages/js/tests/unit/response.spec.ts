//import { expect } from 'expect';

import { createUserCvg } from '../helpers';

describe('unit.response', () => {
  const makerCvg = createUserCvg('maker');
  const takerCvg = createUserCvg('taker');

  it('create', async () => {
    //expect(true).toHaveProperty('address');
  });
});
